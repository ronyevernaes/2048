import { describe, it } from 'vitest';

describe.skip('GridService', () => {
  it('creates a random tile', () => {});
  it('moves a tile towards up when the movement is up', () => {});
  it('moves a tile towards down when the movement is down', () => {});
  it('moves a tile towards left when the movement is left', () => {});
  it('moves a tile towards right when the movement is right', () => {});
  it('adds two tiles with the same value when moved in the same direction', () => {});
  it('not add two tiles with different values when moved in the same direction', () => {});
});
